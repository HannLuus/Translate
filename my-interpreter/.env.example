# Optional: backend URL. If unset or set to a different translate-*.onrender.com host,
# the app uses the canonical URL (see RENDER_BACKEND_URL in src/api.ts). Do not paste a typo here.
# VITE_API_URL=https://translate-u6u1.onrender.com
