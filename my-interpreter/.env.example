# Backend API URL (optional). If unset, app uses the canonical Render URL.
# Canonical URL from Render: MCP list_services → serviceDetails.url (single source of truth).
# VITE_API_URL=https://translate-u6u1.onrender.com
