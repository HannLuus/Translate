# Google Cloud: path to service account JSON (required for Speech-to-Text and Text-to-Speech)
# For local: use a file path. For Render: set GOOGLE_APPLICATION_CREDENTIALS_JSON to the full JSON string.
GOOGLE_APPLICATION_CREDENTIALS=./path-to-your-service-account.json
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}  # optional; used on Render when no file

# Optional: override project ID (otherwise read from the JSON key file)
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Gemini API key (required for translation)
GEMINI_API_KEY=your-gemini-api-key

# Optional: server port (default 3001)
# PORT=3001
